# Resolve label text for a workflow node

Resolve label text for a workflow node

## Usage

``` r
resolve_label(node, node_labels)
```

## Arguments

- node:

  A single row from the workflow data frame

- node_labels:

  Label display mode: "name", "label", or "both"

## Value

Character string of the resolved label text
