# ACP Server Integration for putior

Functions for exposing putior functionality as an ACP (Agent
Communication Protocol) agent, enabling other AI agents to discover and
call putior capabilities via REST API.
