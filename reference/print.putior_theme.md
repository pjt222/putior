# Print a putior_theme Object

Print a putior_theme Object

## Usage

``` r
# S3 method for class 'putior_theme'
print(x, ...)
```

## Arguments

- x:

  A `putior_theme` object.

- ...:

  Additional arguments (ignored).

## Value

The object, invisibly.
